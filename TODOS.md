# YC Roulette — Build TODOs

Generated by /plan-eng-review on 2026-04-03. Updated by /design-review on 2026-04-03.

## Design fixes (from /design-review — already applied to prototype)
- [x] `btn-replay` min-height 44px — was 15px, not tappable
- [x] `fb-cat` / `fb-max` font sizes 8px/9px → 10px on final score card
- [x] `year-range-labels` 10px → 12px
- [x] Placeholder chips: "Funds/Sector" → "Funding/Industry"
- [x] Industry chips: added `aria-label` + `aria-hidden="true"` on emoji icons

## Design polish (deferred from /design-review)
- [ ] **Reveal screen skeleton rows** — pre-render 3 locked reveal rows in placeholder state; flip via CSS transform on reveal. Without this, the screen looks broken for ~600ms. Implement in `game.js` reveal sequence.
- [ ] **Emoji → SVG icons in industry chips** (optional, polish) — emoji render inconsistently cross-platform. Replace with simple inline SVG icons in orange/ink palette. Text-only chips are a fine fallback if time-pressed.

## Pre-build data fix (do first)
- [x] Normalize `"AI / ML"` → `"AI/ML"` in `yc_dataset.js` (10 affected entries)

## Build steps (in order)

### 1. game.js skeleton + state machine
- [x] Create `game.js` with module structure
- [x] Define `state` object: `{ startups[], currentIdx, guesses, scores, totalScore }`
- [x] Wire `<script src="game.js">` into `index.html`

### 2. Session construction
- [x] `buildSession()`: pick 1–2 anchors from famous list
- [x] Fill remaining slots: alive/dead mix (2–3 each), ≤2 per industry
- [x] Retry cap 100, return best attempt on exhaustion

### 3. Screen transitions
- [x] Replace stub `goTo()` or extend to accept startup data and populate DOM
- [x] `showCard(startup)`: populate name, blurb, batch badge
- [x] No back-navigation: remove any back buttons

### 4. Year slider
- [x] Compute `foundingYear = 2000 + parseInt(batch.slice(1), 10)`
- [x] Set slider `min=foundingYear`, `max=2025`
- [x] Set slider default: `Math.round((foundingYear + 2025) / 2)`
- [x] Live year label on `input` event
- [x] Store in `state.guesses.year`

### 5. Funding + industry chips
- [x] Replace stub `selectFund()` with real state write to `state.guesses.funding`
- [x] Replace stub `selectInd()` with real state write to `state.guesses.industry`
- [x] Single-select: deselect others on tap
- [x] Submit gate: enable button only when all 3 guesses non-null

### 6. Scoring engine
- [x] `scoreYear(guess, actual)` → 10 / 8 / 5 / 1
- [x] `scoreFunding(guess, actual)` → 10 / 5 / 0 (tier ordering: seed < <5m < 5-50m < 50m+)
- [x] `scoreIndustry(guess, actual)` → 10 / 3 / 0 (adjacency map + Other=exact-only)
- [x] `?debug=1` gate: expose `runTests()` to console for QA validation

### 7. Reveal sequence
- [x] Replace `revealRows()` hardcoded stub with real data from `state`
- [x] 300ms stagger: year → funding → industry
- [x] Count-up animation (0 → pts, 400ms ease-out)
- [x] Color coding: green=exact, amber=partial, red=miss
- [x] Round subtotal display after all 3 revealed

### 8. Navigation logic
- [x] On "Continue →": if `currentIdx < 4`, advance to next startup → Screen 2
- [x] If `currentIdx === 4`, go to Screen 5

### 9. Final score card
- [x] Compute tier from `totalScore`
- [x] Random one-liner from tier's bank of 5 (no dedup needed)
- [x] 5-startup breakdown: render correct/partial/wrong per guess
- [x] Update `localStorage.yc_roulette_high_score` if new score is higher
- [x] Wrap localStorage in try/catch (silent fail for private browsing)

### 10. Home screen high score
- [x] On page load: read `localStorage.yc_roulette_high_score`, show if exists

### 11. html2canvas share card
- [x] Inject `<script>` for html2canvas on first reveal screen visit (not page load)
- [x] Add `onerror` handler: set flag `html2canvasFailed = true`
- [x] Render off-screen DOM node for clean capture (captures `.final-content` directly, no phone chrome)
- [x] Blank-check: sample 100px; if >90% white/transparent → show toast
- [x] Save as image: html2canvas → PNG → Blob URL → download
- [x] navigator.share(): if available, pre-fill text + URL
- [x] Desktop fallback: clipboard copy + "Copied!" toast

### 12. Play Again
- [x] Full state reset: all guesses, scores, startups, currentIdx
- [x] Rebuild session, go to Screen 1

## QA checklist
- [ ] Full 5-startup round in Chrome, Firefox, Safari (desktop)
- [ ] iOS Safari 15+ (real device if possible)
- [ ] Android Chrome 100+
- [x] Submit button disabled until all 3 guesses made
- [x] Scoring edge cases: validate with `runTests()` in console — 17/17 passed
- [x] Industry adjacency: all 5 pairs + Other — validated by runTests()
- [x] Session construction: log session arrays, verify anchor count + industry mix — verified: 2 anchors, 3 alive, Fintech×2 at limit
- [ ] localStorage: test in private browsing (no error shown)
- [ ] html2canvas: share card non-blank on mobile
- [ ] FCP < 1.5s on throttled 4G (Chrome DevTools)

## Pre-deploy
- [ ] Rename `yc_roulette_prototype.html` → `index.html`
- [ ] Increment `?v=N` on `game.js` and `yc_dataset.js` script tags
- [ ] `vercel --prod`
